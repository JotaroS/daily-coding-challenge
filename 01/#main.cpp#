#include<bits/stdc++.h>

using namespace std;

void solve(int n, int k, vector<int> arr){
  vector<int> arr;
  int n;
  cin>>n;
  for(int i=0; i < n; i++){
    int v; cin>>v;
    arr.push_back(v);
  }

  for(int i=0; i < n;i++){
    for(int j=0; j < n; j++){
      if(i!=j){
	if(a[i]+a[j] == k){
	  return true;
	}
      }
    }
  }
  return false;vector<int> arr;
  int n;
  cin>>n;
  for(int i=0; i < n; i++){
    int v; cin>>v;
    arr.push_back(v);
  }

  for(int i=0; i < n;i++){
    for(int j=0; j < n; j++){
      if(i!=j){
	if(a[i]+a[j] == k){
	  return true;
	}
      }
    }
  }
  return false;
}

int main(void){
  vector<int> arr;
  int n;
  cin>>n;
  for(int i=0; i < n; i++){
    int v; cin>>v;
    arr.push_back(v);
  }

  for(int i=0; i < n;i++){
    for(int j=0; j < n; j++){
      if(i!=j){
	if(a[i]+a[j] == k){
	  return true;
	}
      }
    }
  }
  return false;
}
